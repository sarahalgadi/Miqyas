<!DOCTYPE html>
<html lang="en">

<head>
   <%- include("./partials/head.ejs") %>;
   <link rel="stylesheet" href="/stylesheets/cc.css">
</head>

<body>
    <%- include('./partials/nav.ejs')%>
    <hr>

    <div>
        <div class="row container-fluid">
            <div class="col-md-8">
                <h2 style="font-weight: 600; margin: 1rem; padding: 1rem;"><%= courseName %></h1>
            </div>
            <div class="col-md-4">
                <h2 style="font-size: 20px; margin-left: 40%; margin-top: 1.5rem; padding: 1rem;">Term: <%= term %></h2>
            </div>
        </div>
    </div>

    <div class="container">
        <!--Tabs-->
        <ul class="nav nav-tabs">
            <li class="nav-item">
                <a class="nav-link active tab" data-bs-toggle="tab" href="#editActivites">Edit Activities</a>
            </li>
            <li class="nav-item">
                <a class="nav-link tab" data-bs-toggle="tab" href="#viewReports">View Past Reports</a>
            </li>
            <li class="nav-item">
                <a class="nav-link tab" data-bs-toggle="tab" href="#generateReport">Generate Report</a>
            </li>
        </ul>


        
        <div class="tab-content">
            <!--Specifying activities-->
            <div id="editActivites" class="tab-pane container active p-4 mt-4">
                <h3>Select Assessment Activities to be Considered in CLO Assessment:</h3>
                <hr>
                <form action="/submit-form/<%= title %>/<%= term %>" method="post">

                     <!-- Project -->
                <div class="form-check p-2">
                    <input class="form-check-input" type="checkbox" name="project" id="projectCheckbox" onchange="toggleWeightInput('projectWeight', this.checked)" <%= assignedWeights.project ? 'checked' : '' %>>
                    <label class="form-check-label" for="projectCheckbox">Project</label>
                    <input class="form-control d-none weight" type="number" name="project_weight" id="projectWeight"
                        placeholder="Enter weight (%)" min="0" max="100" value="<%= assignedWeights.project || '' %>">
                </div>

                <!-- Major -->
                <div class="form-check p-2">
                    <input class="form-check-input" type="checkbox" name="major" id="majorCheckbox"
                        onchange="toggleWeightInput('majorWeight', this.checked)" <%= assignedWeights.major ? 'checked' : '' %>>
                    <label class="form-check-label" for="majorCheckbox">Major</label>
                    <input class="form-control d-none weight" type="number" name="major_weight" id="majorWeight"
                        placeholder="Enter weight (%)" min="0" max="100" value="<%= assignedWeights.major || '' %>">
                </div>

                <!-- Assignment -->
                <div class="form-check p-2">
                    <input class="form-check-input" type="checkbox" name="assignment" id="assignmentCheckbox"
                        onchange="toggleWeightInput('assignmentWeight', this.checked)" <%= assignedWeights.assignment ? 'checked' : '' %>>
                    <label class="form-check-label" for="assignmentCheckbox">Assignment</label>
                    <input class="form-control d-none weight" type="number" name="assignment_weight" id="assignmentWeight"
                        placeholder="Enter weight (%)" min="0" max="100" value="<%= assignedWeights.assignment || '' %>">
                </div>

                <!-- Quiz -->
                <div class="form-check p-2">
                    <input class="form-check-input" type="checkbox" name="quiz" id="quizCheckbox"
                        onchange="toggleWeightInput('quizWeight', this.checked)" <%= assignedWeights.quiz ? 'checked' : '' %>>
                    <label class="form-check-label" for="quizCheckbox">Quiz</label>
                    <input class="form-control d-none weight" type="number" name="quiz_weight" id="quizWeight"
                        placeholder="Enter weight (%)" min="0" max="100" value="<%= assignedWeights.quiz || '' %>">
                </div>

                <!-- Final Exam -->
                <div class="form-check p-2">
                    <input class="form-check-input" type="checkbox" name="final_exam" id="finalExamCheckbox"
                        onchange="toggleWeightInput('finalExamWeight', this.checked)" <%= assignedWeights.final_exam ? 'checked' : '' %>>
                    <label class="form-check-label" for="finalExamCheckbox">Final Exam</label>
                    <input class="form-control d-none weight" type="number" name="final_exam_weight" id="finalExamWeight"
                        placeholder="Enter weight (%)" min="0" max="100" value="<%= assignedWeights.final_exam || '' %>">
                </div>

                <!-- Other -->
                <div class="form-check p-2">
                    <input class="form-check-input" type="checkbox" name="other" id="otherCheckbox"
                        onchange="toggleWeightInput('otherWeight', this.checked)" <%= assignedWeights.other ? 'checked' : '' %>>
                    <label class="form-check-label" for="otherCheckbox">Other</label>
                    <input class="form-control d-none weight" type="number" name="other_weight" id="otherWeight"
                        placeholder="Enter weight (%)" min="0" max="100" value="<%= assignedWeights.other || '' %>">
                </div>
                    <!--In case of error in validating the weights, message would be shown here-->
                    <div class="error-text" id="errorText"></div>
                    <button type="submit" onclick="validateWeights()" id="saveButton"
                        class="custom-button mt-4">Save</button>
                </form>
            </div>
            <!--Viewing a report; Ayat this is missing I just placed here the structure-->
            <div id="viewReports" class="tab-pane fade p-4 mt-4">

            </div>
            <!--Generating a report-->
            <div id="generateReport" class="tab-pane fade p-4 mt-4">
                <h3>Generate Report</h3>
                <form id="report" action="/course-report/<%= courseCode %>/<%= term %>" method="get">
                    <p>You can click "Print" to view your prepared course report. </p>
                    <input type="button" value="Print" class="custom-button" id="printButton">
                    <input type="submit" value="Edit" class="custom-button">
                </form>
            </div>
        </div>
    </div>
    </div>
    <script>
        document.getElementById('printButton').addEventListener('click', function () {
          window.location.href = `/view-course-report/<%= courseCode %>/<%= term %>`;
        });
      </script>
    <script src="/scripts/weightValidation.js"></script>
</body>

